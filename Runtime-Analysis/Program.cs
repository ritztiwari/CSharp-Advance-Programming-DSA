using System;

public class Program
{
    public static void Main(string[] args)
    {
        //int size = 100000000;

        //int[] arr = new int[size];

        //for(int i = 0; i < size; i++)
        //{
        //    arr[i] = i;
        //}

        //SearchTargetCompare.SearchCompare(arr, size - 1);

        //Random r = new Random();

        //int size = 10000;

        //int[] arr = new int[size];

        //for (int i = 0; i < size; i++)
        //{
        //    arr[i] = r.Next(1, 10000);
        //}

        //SortingCompare.Compare(arr);

        //StringConcatinationCompare.Compare();

        //FibonacciCompare.Compare(30);

        FileReadingCompare.Compare();
    }
}